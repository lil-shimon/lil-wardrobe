name: lil_wardrobe
description: A new flutter module project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  camera:
  path_provider:
  path:

  amplify_core: '<1.0.0'
  amplify_auth_cognito: '<1.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.example.lil_wardrobe
    iosBundleIdentifier: com.example.lilWardrobe
